# AgentSession Atlas 작업 원칙

- 제품 결정과 현재 상태는 README와 `docs/README.md`에서 확인한다. 상세 설계는 문서 안내의 링크를 따른다. 설계 내용을 구현 완료로 표현하지 않는다.
- README는 글로벌 독자를 위해 영어로 쓴다. `docs/`는 한글로 작성해도 된다. 문장은 짧게 쓰고 반복 정보는 표로 정리한다.
- 변경 전 관련 코드를 읽고, 기존 사용자 변경은 보존한다.
- 기능은 구현·검증·원격 배포 확인까지 구분해서 보고한다.
- 새 의존성과 서비스는 필요성이 확인될 때 추가한다.
- 공통 이벤트 계약은 버전을 관리하고 설치된 구버전 수집기의 호환성을 고려한다.
- 실제 세션·인증정보·환경변수 값은 저장소에 넣지 않는다. 검증에는 합성 데이터를 사용한다.

## 문서와 그림

- 전체 아키텍처는 `docs/README.md` 최상단에, 대시보드 예시는 `docs/atlas.md`에 둔다. Collector는 `docs/collector.md`로 분리하고 배포·운영은 Atlas 문서에 포함한다.
- 상시 갱신하는 설계 문서와 그림 파일명에는 날짜를 넣지 않는다. 외부 정보의 확인 날짜는 본문에 기록한다.
- 서술을 반복하기보다 표와 그림으로 구조·책임·흐름을 보여준다.
- SVG는 `docs/assets/<문서명>-<주제>.svg`에 두며 편집 가능한 text·tspan을 사용한다.
- 아키텍처는 흰 캔버스·파스텔 컴포넌트·남색 글자·직교 연결선을 사용한다.
- 그림의 리전은 `Seoul · icn1`처럼 영문으로 표기한다. 제목·본문·핵심 조건은 글자색과 굵기로 구분하고 예약 작업 제목에는 `30m`·`1h`·`1d` 주기 배지를 붙인다. 구현 상태 안내 같은 관리 문구와 그림에 이미 드러난 내용을 반복하는 부제·각주, 사용하지 않는 기술에 대한 설명은 그림에 넣지 않는다.
- 제품 UI와 새 AI 설명은 한국어 우선이다. 설정에서 한국어/English를 선택한다. 제품 디자인은 다크 기본이며 라이트/다크/시스템 테마를 설정에서 제공하고 두 테마를 검증한다.
- 그림의 기본 서체는 Noto Sans KR(본고딕)이다. 제목은 Bold(700), 본문은 Regular(400)를 사용한다. Zed의 SVG 렌더러 호환성을 위해 가변 폰트 대신 고정 굵기 Regular·Bold 파일을 설치한다.
- 아이콘 출처는 Tech Stack Icons → [techicons.dev](https://techicons.dev/?search=) → Simple Icons 순서로 선택한다. 앞선 출처에 없을 때만 다음 출처를 사용한다.
- 아이콘은 `docs/assets/icons`에 보관하고 SVG 내부에 포함한다. 출처와 라이선스를 보존한다.
- 변경한 SVG는 렌더링해 누락·겹침·잘림을 확인한다. XML 검사와 실제 화면 검증을 구분한다.
- 파일 이동 시 상대 링크·이미지 경로·참고 링크도 함께 갱신한다.
- Codex 오른쪽 미리보기를 자동으로 열지 않는다.

## GitHub

- 이 프로젝트의 구현·검증·배포에 필요한 커밋·푸시·PR 생성과 수정은 사용자에게 별도 확인 없이 진행한다.
- PR assignee는 `@Hyune-c`로 지정한다.
- Draft PR 대신 일반 Open PR을 사용한다.
- PR 생성·수정 뒤 assignee와 Draft 상태를 조회하여 확인한다.
- PR 설명에는 변경 목적·영향·검증을 기록한다.
