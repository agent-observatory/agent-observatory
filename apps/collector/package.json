{
  "name": "@agent-observatory/collector",
  "version": "0.3.0",
  "description": "Codex and Claude Code session collector for AgentSession Atlas",
  "license": "MIT",
  "type": "module",
  "bin": {
    "atlas-collector": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.15.0"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts",
    "prepack": "pnpm build"
  },
  "dependencies": {},
  "devDependencies": {
    "tsup": "^8.5.0",
    "@agent-observatory/contracts": "workspace:*"
  }
}
