{
  "name": "agent-session-atlas",
  "private": true,
  "packageManager": "pnpm@9.12.1",
  "scripts": {
    "build": "pnpm --filter @agent-observatory/contracts build && pnpm --filter @agent-observatory/collector build && pnpm --filter @agent-observatory/web build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prettier": "3.6.2",
    "tsx": "^4.20.0",
    "typescript": "^5.9.3"
  }
}
